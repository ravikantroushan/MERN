// src/utils/api.js
const BASE_URL = 'http://localhost:5000/api'; // Update with your backend URL

const api = {
  login: async (credentials) => {
    // Implement login API request
  },

  register: async (userData) => {
    // Implement register API request
  },

  // Add more API utility functions as needed
};

export default api;
